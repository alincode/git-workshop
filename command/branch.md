# Branch 分支

```
git branch <branch_name>

git show-branch
git branch
git branch -r
git branch -a

git branch -m <old_branch_name> <new_branch_name>

git branch -d <branch_name>
git branch -D <branch_name>
```